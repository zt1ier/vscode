{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run C++ (no debugger)",
            "type": "cppvsdbg",  // Windows native launcher, no GDB spam
            "request": "launch",
            "program": "${fileDirname}\\${fileBasenameNoExtension}.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "console": "integratedTerminal",
            "preLaunchTask": "Build C++ File"
        }
    ]
}
